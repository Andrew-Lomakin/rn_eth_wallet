import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 28,
    paddingTop: 50,
    alignItems: 'center',
    justifyContent: 'space-between',
  },

  address: {
    fontSize: 18,
    textAlign: 'center',
  },

  button: {
    marginBottom: 14,
  },
});
